<div class="row">
  <div class="col-md-6">

    {{#if isEditing}}

      <form>
        <div class="form-group">
          <label for="question">Question:</label>
          {{input id="question" value=question class="form-control"}}
        </div>

        <div class="form-group">
          <label for="author">Author:</label>
          {{input id="author" value=author class="form-control"}}
        </div>

        <div class="form-group">
          <label for="description">Description:</label>
          {{input id="description" value=description class="form-control"}}
        </div>

        <button class="btn btn-success" {{action "save"}}>Post changes</button>
      </form>

    {{else}}

      <h2>{{question}}  <span class="upvotes badge">{{upvotes}} upvotes</span></h2>
      <h4>Asked by: {{author}}</h4>
      <h5>Description: {{description}}</h5>

      <button class="btn btn-warning btn-sm" {{action "edit"}} {{bindAttr disabled="buttonsDisabled"}}>Edit</button>
      <button class="btn btn-danger btn-sm" {{action "delete"}} {{bindAttr disabled="buttonsDisabled"}}>Delete</button>
      <span {{action "newAnswer"}}>{{#link-to "new-answer" this class="btn btn-primary btn-sm"}}Answer{{/link-to}}</span>
      <button class="btn btn-success btn-sm" {{action "upvote"}} {{bindAttr disabled="buttonsDisabled"}}>Upvote</button>

      <br>
      <br>

      <span {{action "displayAnswers"}}> {{#link-to "answers" id}} Display answers {{/link-to}} </span>
      <hr>
      {{#if answersDisplayed}}

        {{outlet "answers"}}

      {{/if}}

    {{/if}}

  </div>

  <div class="col-md-6">
    {{outlet "new-answer"}}
  </div>
</div>
